name: Test cbonds
on:
  push:
    branches: ["main"]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fetch cbonds Oman price
        run: python3 scripts/test-cbonds.py
