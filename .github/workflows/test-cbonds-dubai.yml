name: Test cbonds Dubai
on:
  push:
    branches: ["main"]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Find Dubai index on cbonds
        run: python3 scripts/test-cbonds-dubai.py
